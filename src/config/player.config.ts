import { Sizes } from "../types/common.types";

const PLAYER = {
    SIZES: {
        width: 150,
        height: 90,
    } as Sizes,
    ACCELERATION: 1,
    MAX_VELOCITY: {
        horizontal: 8,
        vertical: 8,
    } 
}

export default PLAYER;