export type Rectangle = { x: number, y: number, width: number, height: number }

export type Textures = {[key: string]: string}


export type AnimationFrame = { textureKey: string, duration: number }; 